<UserControl x:Class="ChatGPTPrompt.PromptSelectionToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             Background="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"
             Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Toggle for sample/existing class -->
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
            <Label Content="Test Class Type:" />
            <RadioButton x:Name="sampleClassRadioButton" Content="Sample Test Class" IsChecked="True" />
            <RadioButton x:Name="existingClassRadioButton" Content="Existing Test Class" />
        </StackPanel>

        <!-- Code to test area -->
        <Label Content="Code to test:" Grid.Row="1" Grid.Column="0" />
        <TextBox x:Name="codeToTestTextBox" Grid.Row="2" Grid.Column="0" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />

        <!-- Sample or existing test class area -->
        <Label Content="Sample/Existing test class:" Grid.Row="3" Grid.Column="0" />
        <TextBox x:Name="testClassTextBox" Grid.Row="4" Grid.Column="0" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />

        <!-- Relevant classes list -->
        <Label Content="Relevant Classes:" Grid.Row="5" Grid.Column="0" />
        <ListBox x:Name="relevantClassesListBox" Grid.Row="6" Grid.Column="0" />
    </Grid>
</UserControl>
